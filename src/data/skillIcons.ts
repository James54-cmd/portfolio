import {
  Activity,
  AppWindow,
  Blocks,
  Bot,
  Box,
  Brain,
  Clock,
  Database,
  FileCode,
  FileCode2,
  FileType,
  Flame,
  GitBranch,
  HeartHandshake,
  Hexagon,
  Layers,
  Layers2,
  Lightbulb,
  MessageCircle,
  MessageSquare,
  MousePointer2,
  Paintbrush,
  PenTool,
  RefreshCw,
  Server,
  Sparkles,
  Table2,
  Users,
  UsersRound,
  Wind,
  Zap,
  type LucideIcon,
} from "lucide-react";

/** Lucide icons keyed for the skills section (pairs with `skills` in portfolio.ts). */
export const SKILL_ICONS = {
  layers: Layers,
  appWindow: AppWindow,
  fileType: FileType,
  wind: Wind,
  fileCode2: FileCode2,
  paintbrush: Paintbrush,
  box: Box,
  blocks: Blocks,
  sparkles: Sparkles,
  server: Server,
  hexagon: Hexagon,
  database: Database,
  table2: Table2,
  layers2: Layers2,
  zap: Zap,
  fileCode: FileCode,
  gitBranch: GitBranch,
  penTool: PenTool,
  mousePointer2: MousePointer2,
  messageSquare: MessageSquare,
  bot: Bot,
  leadership: Users,
  hardworking: Flame,
  communication: MessageCircle,
  collaboration: UsersRound,
  problemSolving: Lightbulb,
  adaptability: RefreshCw,
  timeManagement: Clock,
  empathy: HeartHandshake,
  criticalThinking: Brain,
  accountability: Activity,
} as const;

export type SkillIconKey = keyof typeof SKILL_ICONS;

export function getSkillIcon(key: SkillIconKey): LucideIcon {
  return SKILL_ICONS[key];
}
